#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "hash-table-c-dhash.h"
#include <stdbool.h>

Table * init() {
    return NULL;
}

long hash(Table * table, char * word) {
    return -1;
}

bool insert(Table * table, char * word, long value) {
    return 0;
}

bool lookup(Table * table, char * word) {
    return 0;
}

long get(Table * table, char * word) {
    return -1;
}

bool update(Table * table, char * word, long value) {
    return 0;
}

bool remove(Table * table, char * word) {
    return 0;
}

long size(Table * table) {
    return -1;
}

void print(Table * table) {

}

bool clear(Table * table) {
    return 0;
}